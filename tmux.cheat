% tmux

# Create a new named session
tmux new -s <new_session>

# Attach to a named session
tmux attach -t <existing_session>

$ existing_session: tmux list-sessions --- --column 1 --delimiter ':' --fzf-overrides '--no-select-1'
