% terraform, tf

$ state_address: terraform state list
$ workspace_name: terraform workspace list

# Select a workspace
terraform workspace select <workspace_name>

# Show resource state
terraform state show <state_address>
